[project]
name = "class-8-prac"
version = "0.1.0"
description = "Part 17 capstone: a Gemini-backed CLI agent with two custom tools (OpenAI Agents SDK)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai-agents>=0.0.1",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
